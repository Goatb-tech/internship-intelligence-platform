[project]
name = "internship-intelligence-platform"
version = "0.1.0"
description = "A safe, explainable internship search and application tracking platform"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
internship-intelligence = "internship_intelligence.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
